# Tech Context

## Tech Stack
- **React 19** (`^19.2.6`)
- **Vite 8** (`^8.0.12`) — build tool, dev server, HMR
- **React Router DOM v7** (`^7.17.0`) — client-side routing
- **HeroUI v3** (`@heroui/react`, `@heroui/styles`) — UI primitives (Toast), styles
- **Tailwind CSS v4** (`^4.3.1`, `@tailwindcss/vite`)
- **TypeScript types** installed (`@types/react`) but source is **JavaScript (JSX)** — no TS.

## Key Dependencies
```json
"dependencies": {
  "@heroui/react": "^3.1.0",
  "@heroui/styles": "^3.1.0",
  "@tailwindcss/vite": "^4.3.1",
  "react": "^19.2.6",
  "react-dom": "^19.2.6",
  "react-router-dom": "^7.17.0",
  "tailwindcss": "^4.3.1",
  "tslib": "^2.8.1"
},
"devDependencies": {
  "eslint": "^10.3.0",
  "@vitejs/plugin-react": "^6.0.1",
  "eslint-plugin-react-hooks": "^7.1.1",
  "eslint-plugin-react-refresh": "^0.5.2",
  "globals": "^17.6.0",
  "vite": "^8.0.12"
}
```

## Environment / Config
- **`.env`** sets `BASE_API_URL=https://api-dev-phoenix.monnykapin.com/api/v1`.
- **`vite.config.js`**: `envPrefix: ["VITE_", "BASE_"]` exposes `BASE_API_URL` as `import.meta.env.BASE_API_URL`.
- **Env resolution pattern** (in services): `window.__ENV__?.BASE_API_URL || import.meta.env.BASE_API_URL || ""`. `window.__ENV__` is set by `/env.js` (generated by `env.sh` in Docker/nginx); in local dev `/env.js` 404s, so it falls back to `import.meta.env`.
- **`index.html`**: loads `/env.js` then `/src/main.jsx`.

## Scripts
```json
"scripts": {
  "dev": "vite",
  "build": "vite build",
  "lint": "eslint .",
  "preview": "vite preview"
}
```
- **No `test` script** — no test framework is installed.

## API Endpoints (consumed)
Base: `https://api-dev-phoenix.monnykapin.com/api/v1`

| Resource | Method & Path | Purpose |
|----------|---------------|---------|
| Auth | `POST /auth/login` | Login (email + password) |
| Guests | `GET /guests`, `POST /guests`, `PATCH /guests/:id`, `DELETE /guests/:id` | Monetary contributions |
| Rentals | `GET /rentals` (`?status=&month=&offset=&limit=`), `POST /rentals`, `GET /rentals/:id`, `GET /rentals/:id/status`, `PUT /rentals/:id/status` (`{ status }`), `PUT /rentals/:id`, `POST /rentals/:id/payments`, `GET /rentals/stats`, `DELETE /rentals/:id` | Rental room records |

### Rental status update body
`PUT /rentals/:id/status` accepts `{ status }` (`"pending"`, `"unpaid"`, or `"paid"`). The status-chip dropdown calls this for **Pending**/**Unpaid**; **Paid** instead posts to `/rentals/:id/payments` (see below).

### Payments endpoint body
`POST /rentals/:id/payments` accepts `{ amount, paymentDate }` where:
- `amount` = numeric rent amount (from `rentAmount`).
- `paymentDate` = `YYYY-MM-DD`; year/month from the **month filter** (`?month=`) or the **current month** if none; day from the rental's **dueDate** (or today). Selecting "Paid" in the status-chip dropdown on the rentals page sends this body.

### Rental list response shape
```json
{
  "rentals": [ { "_id", "roomId", "tenantId", "moveInDate", "moveOutDate",
                 "rentAmount", "paymentDate", "dueDate", "paymentStatus",
                 "createdBy", "createdAt", "updatedAt", "__v" } ],
  "total": 2,
  "limit": 10,
  "offset": 0,
  "stats": { "totalRentals", "paid", "pending", "overdue",
             "expectedRent", "collectedRent", "outstandingRent" }
}
```

## Constraints / Gotchas
- **API rate limit: 100 requests / 15 min.** When exceeded, returns `429` WITHOUT CORS headers → browser shows `Failed to fetch`. This is a backend behavior, not fixable in frontend (but caught and re-messaged).
- `roomId`/`tenantId`/`createdBy` may be **populated objects** or `null` — render via `formatReference()`.
- **No error boundary** → render crashes produce a blank white page.
- Local dev: `/env.js` 404 is harmless.

## Tool Usage Patterns
- All app code is plain JSX (no TypeScript).
- Styling is via global CSS classes in `src/App.css` + `src/index.css` (Tailwind v4 import). Components use custom class names, not Tailwind utility classes inline.
